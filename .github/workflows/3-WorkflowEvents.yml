name: 3-WorkflowEvents
#on: push
#on: [push, workflow_dispatch]
on: workflow_dispatch
#  schedule:
#    - cron: '*/5 * * * *' # run every 5 mins
jobs:
  my-first-job:
    name: my first job
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
